Friction = 
{
	tensorContent = (
	
                                //as there are n speeds, fixDimensions can have n different values
                                //structure of fixDimensions: if nonzero, then interpret as coordinate 
                                //along that dimension
                                //dimensions are ordered as: friction, speed, output parameter
                                //elaboratedDimension = 2 -> 3rd dimension is elaborated 
                                //(first dimension is FRICTION, second dimension is SPEED, third dimension is OUTPUT PARAMETER)
                                //here we see all output parameters per fixDimensions 
                                //(totalTime, amplitude, soundTouse, grainSamplingRate, centerPosition, whiteNoiseExponent, decayRatio, reverberation)
								
								//AS THIS VERSION IS ONLY ABOUT SPEED, FRICTION GRADATION DOES NOT REALLY MATTER
	
	
		{ elaboratedDimension = 2;
		fixDimensions = (0, 0, 0);
		values = (3.0, 0.205, 0.542906, 121.053, 0.0, 0.351, 0.391232, 1.0);
		},



		
		///////////////////////////
		///////////////////////////
		


		
                                //elaborated dimension is now 0, so we are specifying one value for each gradation of friction
                                //the value that is specified is the 1st output parameter (totalTime)
                                //again, friction gradation is don't care
		{ elaboratedDimension = 1;
		fixDimensions = (0, 0, 1);
		values = (3.0, 2.7, 2.4, 2.1, 1.8, 1.5, 1.2, 0.9, 0.6, 0.3, 0.2, 0.15, 0.08);
		}		
		
		
	);
};
